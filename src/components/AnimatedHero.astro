---
---

<div class="hero-section min-h-screen flex items-center justify-center relative">
  <div class="container mx-auto px-4 z-10">
    <div class="text-center">
      <slot />
      
      <div class="mt-16 space-y-6 text-xl md:text-2xl">
        <div class="typing-text" style="--delay: 1s">
          <span class="inline-flex items-center justify-center gap-4 hover:transform hover:scale-105 transition-transform">
            <span class="text-3xl">🤖</span>
            <span class="text-gradient font-black tracking-tight">AI & Machine Learning</span>
          </span>
        </div>
        <div class="typing-text" style="--delay: 1.5s">
          <span class="inline-flex items-center justify-center gap-4 hover:transform hover:scale-105 transition-transform">
            <span class="text-3xl">📊</span>
            <span class="text-gradient font-black tracking-tight">Data Engineering</span>
          </span>
        </div>
        <div class="typing-text" style="--delay: 2s">
          <span class="inline-flex items-center justify-center gap-4 hover:transform hover:scale-105 transition-transform">
            <span class="text-3xl">💻</span>
            <span class="text-gradient font-black tracking-tight">Full-Stack Development</span>
          </span>
        </div>
      </div>

      <div class="mt-16 flex gap-6 flex-wrap justify-center">
        <a href="#projects" class="btn-primary">
          View Projects 🚀
        </a>
        <a href="#contact" class="btn-secondary">
          Get in Touch 👋
        </a>
      </div>
    </div>
  </div>
</div>

<style>
  .hero-section {
    background: transparent;
  }

  .text-gradient {
    background: linear-gradient(to right, #fff 20%, #b9bbbe 80%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 200% 100%;
    animation: gradient-shift 8s ease-in-out infinite;
    text-shadow: 0 0 20px rgba(88, 101, 242, 0.5);
    letter-spacing: -0.04em;
  }

  .typing-text {
    opacity: 0;
    transform: translateY(30px);
    animation: fade-in-up 0.8s ease forwards;
    animation-delay: var(--delay);
  }

  @keyframes gradient-shift {
    0%, 100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  @keyframes fade-in-up {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .btn-primary {
    @apply inline-block px-6 py-3 bg-discord-blurple text-white rounded-full font-semibold text-lg
           transition-all duration-300 hover:bg-discord-blurple/80 hover:transform hover:scale-105;
  }

  .btn-secondary {
    @apply inline-block px-6 py-3 bg-discord-dark/50 border-2 border-discord-blurple text-white 
           rounded-full font-semibold text-lg transition-all duration-300 hover:bg-discord-blurple/20 
           hover:transform hover:scale-105;
  }
</style>
